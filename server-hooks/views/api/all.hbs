<h2 class="card-title">Application Programming Interface</h2>
<div class="col-md-12">
    {{#each apis}}
        <div class="row">
            {{# each this}}
                <div class="col-sm-6">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">{{this.name}}</h5>
                            <p class="card-text">{{this.description}}</p>
                            <p><small>Used by # projects</small></p>
                            <a href="/api/view/{{this.aid}}" class="btn btn-primary">Explore projects</a>
                        </div>
                    </div>
                </div>
            {{/each}}
        </div>
    {{/each}}
</div>
